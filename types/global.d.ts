declare type FontStyles = {
  heading: {
    fontSize: string;
    lineHeight: number;
  };
  paragraph: {
    fontSize: string;
  };
};

declare type ImageSize = {
  width: number;
  height: number;
};
